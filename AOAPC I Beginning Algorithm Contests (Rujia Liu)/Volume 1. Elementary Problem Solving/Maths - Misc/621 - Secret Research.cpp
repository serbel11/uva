#include <iostream>
#include <string>
#include <stdio.h>
#include <algorithm>

using namespace std;

int main() {
	int n, q;
	cin >> n;
	while (n--){
		cin >> q;
		if (q == 1 || q == 4 || q = 78){
			cout << "+" << endl;
			break;
		}
	}


	return 0;
}